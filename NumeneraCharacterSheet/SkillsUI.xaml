<Page
    x:Class="NumeneraCharacterSheet.SkillsUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NumeneraCharacterSheet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView HorizontalAlignment="Left" Height="815" Margin="50,50,0,0" VerticalAlignment="Top" Width="549">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Border Background="LightGray" Height="100" Width="200">
                            <TextBlock Text="{Binding}" 
                               FontSize="48" Foreground="Green"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Items>
                <x:String>One</x:String>
                <x:String>Two</x:String>
                <ListViewItem>
                    <TextBox x:Name="Text" Text="Input"/>
                 </ListViewItem>
                
            </ListView.Items>
        </ListView>
    </Grid>
</Page>
